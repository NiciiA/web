import {Connection} from "./options";
import {Gateway} from "./service";
import {GatewayQuery, GatewayQueryBuilder} from "./query";

export {Connection, Gateway, GatewayQuery, GatewayQueryBuilder};

export default new Gateway();
